name: node:ci:javascript

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'packages/javascript/**'

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
    secrets: inherit
    with:
      packageJson: "./packages/javascript/package.json"
