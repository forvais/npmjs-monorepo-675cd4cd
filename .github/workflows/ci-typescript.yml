name: node:ci:typescript

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'packages/typescript/**'

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
    secrets: inherit
    with:
      packageJson: "./packages/typescript/package.json"
