name: node-ci:typescript

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'packages/typescript/**'

jobs:
  node-ci:
    uses: ./.github/workflows/node-ci.yml
    secrets: inherit
    with:
      root: ./packages/javascript
